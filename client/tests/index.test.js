import { describe, it, expect } from 'vitest';
import { getVersion } from '../src/index.js';

describe('stock-analyst-client', () => {
  it('placeholder: should pass', () => {
    // TODO: Replace with real specs after /speckit.specify and /speckit.plan
    expect(getVersion()).toBe('0.1.0');
  });
});
